{
    "private": true,
    "scripts": {
        "build": "node-sass ./sass/ -x -r -o ./build/ --output-style compressed",
        "watch": "node-sass ./sass/ -w -x -r -o ./build/ --output-style compressed",
        "postinstall": "husky install"
    },
    "devDependencies": {
        "@commitlint/cli": "^11.0.0",
        "@commitlint/config-conventional": "^11.0.0",
        "husky": "^5.0.9",
        "node-sass": "^4.12.0"
    }
}
