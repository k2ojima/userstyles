{
    "private": true,
    "scripts": {
        "build": "node-sass ./_sass/ -x -r -o ./ --output-style compressed",
        "watch": "node-sass ./_sass/ -w -x -r -o ./ --output-style compressed"
    },
    "devDependencies": {
        "node-sass": "^4.12.0"
    }
}
